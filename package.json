{
  "name": "senior-dev-kit",
  "version": "2.2.1",
  "description": "Agents, skills, rules and guardrails that make Claude Code behave like a senior engineering team. Installed as a Claude Code plugin or into ~/.claude.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "author": "Mehmet Türkan",
  "homepage": "https://github.com/mtvrkan/senior-dev-kit#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mtvrkan/senior-dev-kit.git"
  },
  "bugs": {
    "url": "https://github.com/mtvrkan/senior-dev-kit/issues"
  },
  "keywords": [
    "claude-code",
    "claude-code-plugin",
    "ai-agents",
    "agent-skills",
    "developer-tools"
  ],
  "engines": {
    "node": ">=24.0.0"
  },
  "seniorDevKit": {
    "//": "Single source of truth for the Node floor `scripts/install.mjs` must run on. It is deliberately lower than engines.node: engines covers the TypeScript validators a contributor runs, the installer runs on whatever Node a new user already has. Both READMEs' stated floor, lib/install-core.mjs's header note, and the installer-compat CI job are checked against this value by scripts/check-consistency.ts.",
    "installerNodeFloor": "18"
  },
  "scripts": {
    "test": "node --experimental-strip-types --test scripts/validate-skills.test.ts scripts/deny-cost.test.ts scripts/run-checks.test.ts scripts/install.test.mjs scripts/session-context.test.mjs",
    "setup": "node scripts/install.mjs",
    "validate": "node --experimental-strip-types scripts/validate-skills.ts",
    "link-check": "node --experimental-strip-types scripts/check-links.ts",
    "consistency-check": "node --experimental-strip-types scripts/check-consistency.ts",
    "routing-eval": "node --experimental-strip-types scripts/routing-eval.ts",
    "check-plugin": "node --experimental-strip-types scripts/check-plugin.ts",
    "deny-cost": "node --experimental-strip-types scripts/deny-cost.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint scripts/",
    "markdown-lint": "markdownlint-cli2 \"**/*.md\" \"!node_modules\" --config .markdownlint.json",
    "audit": "npm audit --audit-level=moderate",
    "check-release": "node --experimental-strip-types scripts/check-release.ts",
    "check": "node --experimental-strip-types scripts/run-checks.ts"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^24.13.3",
    "eslint": "^10.6.0",
    "markdownlint-cli2": "^0.23.1",
    "typescript": "^5.8.0",
    "typescript-eslint": "^8.62.1"
  }
}
