name: Bug report
description: Something in the kit misbehaves — install, validation, routing, a skill, or docs.
labels: [bug]
body:
  - type: dropdown
    id: area
    attributes:
      label: Area
      options:
        - Install (plugin marketplace / SETUP.md walkthrough)
        - Routing (wrong agent / tier)
        - Skill or command behavior
        - Hooks
        - Validation scripts (npm run check)
        - Settings / deny rules
        - Documentation
        - Other
    validations:
      required: true
  - type: textarea
    id: what
    attributes:
      label: What happened
      description: What did you ask / run, and what did the kit do instead of the expected behavior?
      placeholder: |
        Prompt or command: ...
        Expected: routed to db-guard with a plan
        Actual: senior-engineer edited the migration directly
    validations:
      required: true
  - type: textarea
    id: doctor
    attributes:
      label: kit-doctor output
      description: Run `/kit-doctor` (or the SETUP.md Step 6 verify checklist) and paste the verdict — it pins down install-state issues immediately.
      render: text
  - type: input
    id: version
    attributes:
      label: Kit version
      placeholder: v2.0.0 (see CHANGELOG.md)
    validations:
      required: true
  - type: input
    id: platform
    attributes:
      label: Platform
      placeholder: Windows 11 / macOS 15 / Ubuntu 24.04 — plus your shell (PowerShell/bash/zsh) if the issue is hook- or script-related
