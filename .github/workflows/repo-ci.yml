name: Repo CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

permissions:
  contents: read

jobs:
  markdown-lint:
    name: Markdown Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - name: Run markdownlint
        uses: DavidAnson/markdownlint-cli2-action@05f32210e84442804257b2a6f20b273450ec8265  # v16.0.0
        with:
          globs: |
            **/*.md
            !node_modules
          config: .markdownlint.json

  yaml-lint:
    name: YAML Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - name: Run yamllint
        uses: ibiqlik/action-yamllint@2576378a8e339169678f9939646ee3ee325e845c  # v3.1.1
        with:
          file_or_dir: security/ .github/
          config_data: |
            extends: default
            rules:
              line-length:
                max: 200
              truthy:
                allowed-values: ['true', 'false']

  typecheck:
    name: TypeScript Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - uses: actions/setup-node@cdca7365b2dadb8aad0a33bc7601856ffabcc48e  # v4.3.0
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run TypeScript type check
        run: npm run typecheck

  eslint:
    name: ESLint (scripts/)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - uses: actions/setup-node@cdca7365b2dadb8aad0a33bc7601856ffabcc48e  # v4.3.0
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint on scripts/
        run: npm run lint

  routing-eval-static:
    name: Routing Eval (static)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - uses: actions/setup-node@cdca7365b2dadb8aad0a33bc7601856ffabcc48e  # v4.3.0
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Validate golden prompts against agents on disk
        run: npm run routing-eval

  validate-skills:
    name: Validate Skill Frontmatter
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - uses: actions/setup-node@cdca7365b2dadb8aad0a33bc7601856ffabcc48e  # v4.3.0
        with:
          node-version: '22'

      - name: Run unit tests
        run: node --experimental-strip-types --test scripts/validate-skills.test.ts scripts/install.test.ts

      - name: Run skill validator
        run: node --experimental-strip-types scripts/validate-skills.ts

  shellcheck:
    name: Shell Script Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - name: Run ShellCheck on install.sh
        uses: ludeeus/action-shellcheck@00cae500b08a931fb5698e11e79bfbd38e612a38  # 2.0.0
        with:
          scandir: '.'
          additional_files: 'install.sh'
          severity: warning

  psscriptanalyzer:
    name: PowerShell Script Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - name: Install PSScriptAnalyzer
        shell: pwsh
        run: Install-Module -Name PSScriptAnalyzer -Force -Scope CurrentUser -ErrorAction Stop

      - name: Run PSScriptAnalyzer on install.ps1
        shell: pwsh
        run: |
          $results = Invoke-ScriptAnalyzer -Path install.ps1 -Severity Warning,Error
          if ($results) {
            $results | Format-Table RuleName, Severity, Line, Message -AutoSize
            Write-Error "PSScriptAnalyzer found $($results.Count) issue(s) in install.ps1"
            exit 1
          }
          Write-Host "PSScriptAnalyzer: no issues found in install.ps1."

  # ShellCheck/PSScriptAnalyzer only lint the installers; this matrix actually
  # executes them end-to-end on all three OSes. The windows leg pins the tests
  # to Windows PowerShell 5.1 — the oldest shell install.ps1 must support. The
  # macOS leg guards install.sh against bash-4-only syntax (macOS ships 3.2).
  install-e2e:
    name: Install Script E2E (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
          - os: macos-latest
          - os: windows-latest
            install-test-shell: powershell
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - uses: actions/setup-node@cdca7365b2dadb8aad0a33bc7601856ffabcc48e  # v4.3.0
        with:
          node-version: '22'

      - name: Run installer end-to-end tests
        env:
          INSTALL_TEST_SHELL: ${{ matrix.install-test-shell }}
        run: node --experimental-strip-types --test scripts/install.test.ts

  check-links:
    name: Check Internal Markdown Links
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - uses: actions/setup-node@cdca7365b2dadb8aad0a33bc7601856ffabcc48e  # v4.3.0
        with:
          node-version: '22'

      - name: Verify internal markdown links and anchors resolve
        run: node --experimental-strip-types scripts/check-links.ts

  check-stale-presets:
    name: Check Stale Presets and Rules
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - uses: actions/setup-node@cdca7365b2dadb8aad0a33bc7601856ffabcc48e  # v4.3.0
        with:
          node-version: '22'

      - name: Flag presets and rules not reviewed in the last 365 days
        env:
          STALE_AFTER_DAYS: '365'
        run: node --experimental-strip-types scripts/check-stale.ts

  check-sha-pins:
    name: Check SHA Pins
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - name: Detect unpinned actions
        run: |
          set -euo pipefail
          found=0
          scanned=0
          while IFS= read -r -d '' file; do
            scanned=$((scanned + 1))
            if grep -E "uses: .+@(v[0-9]+(\.[0-9]+){0,2}|latest|main|master)([[:space:]]|#|$)" "$file" > /dev/null 2>&1; then
              echo "UNPINNED ACTION in $file:"
              grep -nE "uses: .+@(v[0-9]+(\.[0-9]+){0,2}|latest|main|master)([[:space:]]|#|$)" "$file"
              found=1
            fi
          done < <(find .github/workflows security/workflows \( -name "*.yml" -o -name "*.yaml" \) -print0)
          # Guard against a vacuous pass: if the find paths moved and nothing was
          # scanned, fail loudly instead of reporting "All actions are SHA-pinned."
          if [ "$scanned" -eq 0 ]; then
            echo "No workflow files found to scan — check the find paths above." >&2
            exit 1
          fi
          if [ "$found" -eq 1 ]; then
            echo ""
            echo "Pin all actions to a full commit SHA (not mutable tags)."
            exit 1
          fi
          echo "All actions are SHA-pinned (${scanned} workflow files scanned)."

      # A pin that LOOKS like a SHA but doesn't exist upstream (typo, hand-edited,
      # hallucinated) passes the format check above yet kills every run with
      # "Unable to resolve action" — verify each one actually resolves.
      - name: Verify pinned SHAs resolve upstream
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          bad=0
          checked=0
          while IFS= read -r action; do
            repo=$(echo "$action" | cut -d@ -f1 | cut -d/ -f1-2)
            sha=$(echo "$action" | cut -d@ -f2)
            checked=$((checked + 1))
            code=$(curl -s -o /dev/null -w "%{http_code}" \
              -H "Authorization: Bearer ${GH_TOKEN}" \
              "https://api.github.com/repos/${repo}/commits/${sha}")
            if [ "$code" != "200" ]; then
              echo "UNRESOLVABLE PIN (HTTP ${code}): ${action}"
              bad=1
            fi
          done < <(grep -rhoE "uses: [^ ]+@[a-f0-9]{40}" .github/workflows security/workflows | sed 's/uses: //' | sort -u)
          if [ "$checked" -eq 0 ]; then
            echo "No pinned actions found to verify — check the grep above." >&2
            exit 1
          fi
          if [ "$bad" -eq 1 ]; then
            echo ""
            echo "Fix by resolving the intended version tag to its real commit SHA:"
            echo "  curl -s https://api.github.com/repos/<owner>/<repo>/commits/<tag> | jq -r .sha"
            exit 1
          fi
          echo "All ${checked} pinned SHAs resolve upstream."
